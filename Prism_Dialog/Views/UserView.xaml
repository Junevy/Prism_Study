<Page x:Class="Prism_Dialog.Views.UserView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Prism_Dialog.Views"
      mc:Ignorable="d" 
      xmlns:vm="clr-namespace:Prism_Dialog.ViewModels"
      d:DataContext="{d:DesignInstance Type=vm:UserViewModel}"
      ShowsNavigationUI="False"
      Height="300" Width="500"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserView">

    <StackPanel>
        <TextBlock Text="{Binding Message}" FontSize="20"/>
        
        <TextBlock Text="{Binding User.Id}" FontSize="20"/>
        <TextBlock Text="{Binding User.Name}" FontSize="20"/>
        <TextBlock Text="{Binding User.Age}" FontSize="20"/>

        <Button Height="30" Width="110" Command="{Binding ChangeUserInfo}" Content="Change User Info"/>
    </StackPanel>
</Page>
