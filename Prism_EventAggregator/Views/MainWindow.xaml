<Window x:Class="Prism_EventAggregator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525" >
    <StackPanel>
        <Button Width="120" Height="30" Content="Open Admin" Command="{Binding OpenAdminDialog}"/>
        <Button Width="120" Height="30" Content="Opem User" Command="{Binding OpenUserDialog}"/>
        
        <Button Width="120" Height="30" Content="Publish Msg" Command="{Binding PublishCommand}"/>
    </StackPanel>
</Window>
